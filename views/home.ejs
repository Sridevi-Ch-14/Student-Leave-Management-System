<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Student Leave Management System</title>
  <style>
      body {
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  background-color: #f5f5f5;
  color: #333;
  margin: 0;
  padding: 0;
}

    .hero {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
      padding: 2rem;
      text-align: center;
      background: linear-gradient(to right, #e0f7fa, #fce4ec);
    }

    .hero h1 {
      font-size: 3rem;
      color: #2c3e50;
      margin-bottom: 1rem;
    }

    .hero p {
      font-size: 1.2rem;
      max-width: 600px;
      margin-bottom: 2rem;
      color: #555;
    }

    .buttons {
      display: flex;
      gap: 1rem;
      flex-wrap: wrap;
      justify-content: center;
    }

    .btn {
      padding: 0.75rem 1.5rem;
      font-size: 1rem;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      text-decoration: none;
      transition: background 0.3s ease;
    }

    .btn-primary {
      background-color: #007bff;
      color: white;
    }

    .btn-primary:hover {
      background-color: #0056b3;
    }

    .btn-secondary {
      background-color: #e0e0e0;
      color: #333;
    }

    .btn-secondary:hover {
      background-color: #cfcfcf;
    }
  </style>
</head>
<body>
  <div class="hero">
    <h1>Student Leave Management System</h1>
    <p>
      A simple and secure platform for Students and Faculty to manage leave applications.<br />
      Students: Just login, fill out a short form, and wait for approval.<br />
      Faculty: Review requests and approve or reject in seconds.
    </p>

    <div class="buttons">
      <a href="/login" class="btn btn-primary">Login</a>
      <a href="/register" class="btn btn-secondary">Register</a>
    </div>
  </div>

  <footer>
    <p>&copy; <%= new Date().getFullYear() %> Student Leave Management System</p>
  </footer>
</body>
</html>
